using System;

/// <summary>
/// ServiceLocator has been retired in favor of VContainer dependency injection.
/// Keeping this stub triggers a compile-time error if legacy usage slips back in.
/// </summary>
[Obsolete("ServiceLocator no longer exists. Inject dependencies via VContainer instead.", true)]
public static class ServiceLocator
{
}
