using UnityEngine;

namespace Santa.Infrastructure.Combat
{
    /// <summary>
    /// Marker component to identify GameObjects that contain level-specific visual elements.
    /// </summary>
    public class LevelVisuals : MonoBehaviour
    {
        // Marker component. No additional code needed.
    }
}
